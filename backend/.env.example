# Sonote Backend Environment Variables
# Copy this file to .env and configure your values

# ========== Server ==========
HOST=0.0.0.0
PORT=3001
DEBUG=true

# ========== CORS ==========
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ========== Mock Mode ==========
# Set to false to enable real API calls
# When true: uses simulated responses (no API calls)
# When false: uses real ASR and LLM APIs
USE_MOCK=true

# ========== DashScope (Aliyun) ==========
# Get API key from: https://dashscope.console.aliyun.com/
# Used for both ASR (Paraformer) and LLM (Qwen)
# Required for: transcription + text polishing
DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# ========== DeepSeek (Alternative LLM) ==========
# Get API key from: https://platform.deepseek.com/
# Optional: alternative LLM provider
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ========== LLM Provider ==========
# Which LLM to use for text polishing
# Options: dashscope (Qwen-Max) | deepseek (DeepSeek-Chat)
LLM_PROVIDER=dashscope
